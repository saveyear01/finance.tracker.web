# Base URL of the API.
# Leave as `/api` in development so the Vite proxy forwards to FastAPI on :8000
# and the auth cookies stay same-origin.
VITE_API_URL=/api

VITE_APP_NAME=Finance Tracker

# ISO 4217 code every amount is shown in. One currency for the whole app.
VITE_CURRENCY=PHP
